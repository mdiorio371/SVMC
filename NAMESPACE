# Hand-maintained NAMESPACE for SVMC 0.1.0.
#
# NOTE: this file is written by hand, not generated by roxygen2. Only
# R/cohesion.R currently carries roxygen comments, so running
# devtools::document() now would REGENERATE this file from @export tags and
# drop every export except the cohesion functions. Do not run document()
# until @export has been added to every function you want exported.

# ---- exported functions --------------------------------------------------
export("%||%")
export(coalesce_chr)
export(file_exists_at_url)

export(dnaA_from_gff)
export(dnaA_sync)
export(id_dnaA_boxes)
export(locate_OriC)
export(compute_disparities)
export(GC_check_R)

export(get_dists)
export(mash_commands)
export(get_pairwise_identities)

export(simple_nucmer)
export(generate_nucmer_commands)
export(read_delta)
export(filter_delta)
export(plot_delta)
export(benchmark_alignments)

export(assign_domains)
export(delta_indels)
export(delta_duplications)
export(delta_translocations)
export(delta_structural_rearrangements)
export(delta_substructural_inversions)
export(delta_all_SVs)
export(get_all_SVs)
export(group_svs)

export(assign_kde_peaks)
export(recursive_gmm)

export(read_prokka_gff2)
export(annotate_SVs)

export(svmc_cohesion)
export(svmc_cohesion_screen)

# ---- imports --------------------------------------------------------------
# Wholesale imports for packages whose functions are called unqualified,
# so no bare call can resolve to the wrong namespace. dplyr is imported last
# so its verbs win any name clash with the Bioconductor packages above.
import(Biostrings)
import(GenomicRanges)
import(IRanges)
import(S4Vectors)
import(GenomeInfoDb)
import(ggplot2)
import(dplyr)

importFrom(tibble, as_tibble)
importFrom(data.table, fread)
importFrom(ape, read.gff)
importFrom(pracma, findpeaks)
importFrom(RColorBrewer, brewer.pal)
importFrom(pbmcapply, pbmclapply)
importFrom(stats, density, dist, quantile, setNames)
importFrom(utils, tail, write.csv)

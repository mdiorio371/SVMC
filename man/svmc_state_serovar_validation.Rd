% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/17-validation.R,
%   R/svmc_state_serovar_validation.R
\name{svmc_state_serovar_validation}
\alias{svmc_state_serovar_validation}
\title{Validate structural-variant states against an external typing scheme}
\usage{
svmc_state_serovar_validation(M, serovar, state_map, lineage_map)

svmc_state_serovar_validation(M, serovar, state_map, lineage_map)
}
\arguments{
\item{M}{presence matrix (genomes x motifs); carrier if entry > 0.}

\item{serovar}{named character vector (names = genome IDs) or a data frame
with columns `qid` and `serotype`, giving each genome's reference call.}

\item{state_map}{named character vector: names are motif UIDs (columns of
`M`), values are human-readable state labels.}

\item{lineage_map}{named list: names are lineage labels, values are
character vectors of serovar names composing that lineage.}
}
\value{
A tibble with one row per lineage: `lineage`, `state`, precision
  (`precision_n`, `precision_d`, `precision_pct`) and recall (`recall_n`,
  `recall_d`, `recall_pct`).

a tibble with one row per state/lineage: precision (n/d, pct) and
  recall (n/d, pct).
}
\description{
For each structural-variant *state* -- defined by a marker motif whose
carriers constitute the state -- computes precision and recall against a
reference lineage assignment (for example, SeqSero2 serovar calls). This is
the independent test of whether a structurally-defined state corresponds to
a real biological lineage.

For each structural-variant state (defined by a marker motif), computes
precision and recall against a reference lineage/serovar assignment.
Precision = fraction of state carriers belonging to the target lineage;
recall = fraction of the target lineage's genomes that carry the state.
}
\details{
\strong{Precision} = fraction of the state's carriers that belong to the
target lineage (does carrying the inversion predict the lineage?).
\strong{Recall} = fraction of the target lineage's genomes that carry the
state (does the inversion capture the whole lineage, or a sublineage?).

The exclusivity partition (from [svmc_inversion_exclusivity()]) is computed
from structure alone; the reference typing here is independent data, so a
state that scores high precision is a genuine structure-to-lineage
correspondence, not a circular restatement.
}
\examples{
ids <- paste0("g", 1:8)
M <- matrix(0, 8, 2, dimnames = list(ids, c("mA", "mB")))
M[1:4, "mA"] <- 1        # state A carriers
M[5:7, "mB"] <- 1        # state B carriers
sero <- c(g1="X", g2="X", g3="X", g4="Y",   # A is mostly serovar X
          g5="Z", g6="Z", g7="Z", g8="X")   # B is serovar Z
state_map <- c(mA = "A", mB = "B")
lineage_map <- list(
  "X-lineage" = list(state = "A", serovars = "X"),
  "Z-lineage" = list(state = "B", serovars = "Z")
)
svmc_state_serovar_validation(M, sero, state_map, lineage_map)

}
\seealso{
[svmc_inversion_exclusivity()]
}

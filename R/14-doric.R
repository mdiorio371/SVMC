# ==========================================================================
# SVMC: DoriC integration helpers
# Cross-reference predicted OriC positions against the DoriC database.
# ==========================================================================
#
# TODO(SVMC): implement DoriC integration helpers (see handoff 'Known gaps' #3):
#   - look up DoriC OriC position for an accession or species
#   - compute circular offset between two positions on a chromosome
#   - wrapper for the full DoriC cross-reference workflow
#
# Context: B. pertussis dnaA sits ~330 kbp from the DoriC-predicted OriC;
# RASR symmetry analyses must be re-anchored to true OriC.
